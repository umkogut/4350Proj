<!DOCTYPE html>
<html lang="en">
{% extends "index.jinja2" %}

{% block body %}
<header class="subhead">
  <div class="container">
    <h1>Menu</h1>
    <p class="lead">This is where you create and manipulate items in your menu.</p>
  </div>
</header>

<div class="container">
	<dl>
		<table class="table">
			{% for category in ('Appetizers', 'Entrees', 'Dessert') %}
			<tbody>
				<tr>
					<td colspan="2"><h4><dt>{{category}}</dt></h4></td>
				</tr>
				{% for item in menuItems if item.category == category %}
				<tr>
					<td><dd>{{item.name}}</dd></td>
					<td>{{"$%.2f"|format(item.price)}}</td>
				</tr>
				{% endfor %}
			</tbody>
			{% endfor %}
		</table>
	</dl>
</div> <!-- container -->
{% endblock body %}


